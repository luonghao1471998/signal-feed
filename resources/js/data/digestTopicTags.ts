/** Static mock topic tags for digest filter bar (OR match on signal.tags). */
export const DIGEST_TOPIC_TAGS = [
  "#model-release",
  "#tool-launch",
  "#market-data",
  "#pricing",
  "#ai-coding",
  "#fintech",
] as const;
