/**
 * DEPRECATED — Task 1.10.2: mock data removed; dữ liệu lấy từ GET /api/signals.
 * Giữ re-export types để tương thích import cũ.
 */
export type { DigestSignal, KOLSource } from "@/types/digestUi";
